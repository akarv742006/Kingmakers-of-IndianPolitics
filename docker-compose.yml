version: '3.8'

services:
  mandate-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mandate_national_server
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: always
